Flask==2.1.2
phi-tools==0.1.0  # Replace with the latest version if available
yfinance==0.1.70  # Replace with the latest version if available
requests==2.26.0  # Replace with the latest version if available
